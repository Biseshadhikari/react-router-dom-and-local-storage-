import React, { useContext } from 'react';
import Child from './children/Child'; // Assuming this is correct

function Parent() {

    
  return (
    <div>
        <Child/>
    </div>
  );
}

export default Parent;
